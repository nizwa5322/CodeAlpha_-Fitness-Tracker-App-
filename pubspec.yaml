name: task4_fitnesstrackerapp
description: A fitness tracking app with dashboard and activity logging
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Database
  sqflite: ^2.3.0
  path: ^1.8.3
  
  # UI & Utilities
  intl: ^0.18.1
  # fl_chart: ^0.63.0  # REMOVED - causing issues

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true